# 更新记录 · Changelog

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。日期是打包日期。
每一项功能怎么用，见[使用手册](corridor-newtab/README.md)。

---

## [1.21.0] — 2026-09-16

新增「暂歇」：一键把长廊关掉，这一页换成你常去的那几个站点。

**新增**

- **暂歇**：顶栏设置左边一个开关（快捷键 `Q`）。不展画，改成一块安静的落脚地；
  再点一次回到展出。状态跨标签页同步，重启浏览器也还是关着
- 三种排版：**展签墙**（默认，墙与灯都还在，挂的是签不是画）、**闭馆告示**、**目录索引**
- 展签墙两种皮：**简约**（奶油纸）与**彩签**（一排签从左到右铺成一柄色卡扇面）
- 闭馆告示与目录索引可以选**底色**：跟随设置面板 / 浅 / 深
- 站点两处合流：**自己钉的**（存本机，零权限）＋ **Chrome 的常访问榜**
  （`topSites`，**可选权限**，第一次开启时才申请，随时能撤回）
- 墙上末尾一张「＋ 钉一个站点」，就地展开表单；自己钉的那几张指上去有个 × 能撤
- 这一屏不取站点图标、不发任何网络请求

**修复**

- 一打开就处于暂歇状态时，启动遮罩不消失，整页只剩「CORRIDOR」
- 「最多显示几个」把自己钉的也一起削，钉满之后再钉一个看着像没反应；
  现在那个数只削 Chrome 那张榜，自己钉的一定显示
- 站点名截得更干净：「扣子编程，你的 AI 开发伙伴，…」显示成「扣子编程」

## [1.20.0] — 2026-09-15

本机模型终于连得上了；多了「去除」与「备份恢复」。

**新增**

- **备份与恢复**（设置 → 存储）：配置装进一个 .json，重装或换机器一导就回来。
  哪几块带走逐项勾选；导入可选**覆盖**或**并入**。
  接口密钥三条路：不带 / **口令加密**（PBKDF2 + AES-GCM，不引第三方库）/ 明文
- **去除**（快捷键 `X`）：不再轮换也不列进藏品库。只是一份名单，
  设置里随时放回，点完还有五秒可撤销
- 本机模型那一档会提示「不需要密钥」；地址写成 `0.0.0.0` / `::1` 会劝改成 `127.0.0.1`

**修复**

- **Ollama 一直连不上（HTTP 403 · 未知错误）**。真因是它在服务端校验 `Origin`，
  而浏览器给扩展打的来源不在白名单里 —— 跟密钥、模型权限都无关。
  拿到该地址的权限后，长廊会把发往**本机地址**的请求的来源改写成那个地址自己；
  万一走不通，诊断卡会按系统给出可复制的 `OLLAMA_ORIGINS` 命令
- 今日画夹作品多或竖幅高时滚不动，下半截看不到
- 「关于」那行快捷键漏了 `M`（模式）、`D`（下载）、`Esc`（关闭面板）
- 文案订正：内置 106 幅，但办公模式默认开着，**默认展出 93 幅**

## [1.19.1] — 2026-09-12

三个「看着像没反应」的交互死局。

- 一个接口都没有时，AI 补全里建不出第一个接口档
- 按 `L` 打开藏品库，那个 `l` 会被打进随即聚焦的搜索框
- 承上：框里有字时第一下 Esc 只退出输入，面板不关，字母快捷键跟着一起像失灵

## [1.19.0] — 2026-09-10

这一版全部围绕「墙」。

**新增**

- 墙面颜色可以自己配（色卡 + 自定义色板），先把「看不清」这件事解决掉
- 墙色的饱和度与色温可微调
- 灯光：亮度、角度、冷暖、灯数（0–4）
- 墙面材质推倒重做，新增金属背景板一组

## [1.18.0] — 2026-09-10

上架前的安全与文案整理。

- **修复三处漏转义**（真会出事的那种）
- CSP 从两条收紧到十条
- 53 处文案返工

## [1.17.0] — 2026-09-10

自定义图库从「一个文件夹」变成「一串来源」。

- 可加多个本机文件夹，也可加**在线图库的网址**（目录页、图库页、JSON 清单、
  一行一个网址的文本、单张图片都认）
- 每个来源各带一套筛选：只要 / 排除（通配符或正则）、格式、最小边长

## [1.16.0] — 2026-09-10

- 「这个模型能不能识图」改成**真发一次请求实测**，不再靠模型名去猜
- 文字（翻译）与识图（补全）可以各配各的接口

## [1.15.0] — 2026-09-10

- 接口报错从「原样甩给你」变成「告诉你哪儿不对、该怎么改」
- 语言改成「母语 + 外语」两个位置

## [1.14.4] — 2026-09-09

- 修复：底部那排按钮的悬停提示落在屏幕外，只看得见上半截。改成往上冒

## [1.14.3] — 2026-09-09

- 修复：翻墙签语言时画作被推着左右晃（给墙签留了一格恒定宽度的位置）
- 墙签点一下是真的翻面

## [1.14.2] — 2026-09-09

- 设置标题栏那一排四个控件统一尺寸与圆角，按使用频率重排
- 「设置范围」默认改成「当前」，并且存进设置（以前关掉标签页就忘）

## [1.14.1] — 2026-09-09

- 新增：一个接口下可存多把密钥与多个模型；每日几幅可调；
  「静止时隐藏」拆到单项；墙签翻面；设置范围开关
- **撤掉 v1.14.0 的「插件总开关」**：Chrome 不允许扩展临时摘掉新标签页接管，
  所谓「关闭」只能自己渲染一张留白页假装让位，是个半成品

## [1.13.1] — 2026-08-31

- 设置面板能切浅色
- 补全失败可以重试，也能只重来某一张
- 修复：「关于」上面多出一条重复的分隔线

## [1.13.0] — 2026-08-31

- 设置抽屉重做：五页、每组可折叠、能搜
- 接口可以存好几套；定时与张数都能填自定义值

## [1.12.1] — 2026-08-31

- 补全过程看得见：逐张清单，哪张补了几项、哪张失败在哪一步
- 三份提示词摊开来可改

## [1.12.0] — 2026-08-31

- **新增 AI 补全**：接你自己的多模态模型，把「每日新作」与「自定义图库」的空字段补上
- 名作与普通图片各用一套提示词 —— 只写一套时，模型会把厨房照片说成「荷兰黄金时代静物」

## [1.11.14] — 2026-08-30

- 修复：瀑布流进场先糊一会儿。它是固定 72 幅在循环，懒加载没有意义，改成一次全取

## [1.11.13] — 2026-08-30

- 修复：刚进瀑布流时上面一片空白（占位图挂在被隐藏的 `<img>` 上，跟着一起没了）

## [1.11.12] — 2026-08-30

- 设置里的说明文字整体收短，中英同步

## [1.11.11] — 2026-08-29

- 修复：胶卷只有中间几幅正常、左右被重叠切割（一个符号错，在片门附近正好抵消）

## [1.11.10] — 2026-08-29

- 胶卷曲线重做为单程山谷形，从「一堆错位的碎卡片」回到一条平滑连续的片子

## [1.11.9] — 2026-08-29

- 胶卷回到一幅焦点图，屏内可见约 9–10 幅

## [1.11.8] — 2026-08-29

- 胶卷改成「三格平顶 + 陡降卷曲」

## [1.11.7] — 2026-08-29

- 胶卷按经典胶卷剪贴画重摆：拱形姿态、亮黑光泽片基、画格白色帧线

## [1.11.6] — 2026-08-29

- 胶卷有了多重卷曲与远近节奏

## [1.11.5] — 2026-08-29

- 胶卷按真实形态重摆：片门那一段完全水平、零转角，焦点图是严格的平面矩形

## [1.11.4] — 2026-08-23

- 胶卷有了焦点图：片门那一格离眼睛最近、也最大

## [1.11.3] — 2026-08-23

- 胶卷不卡了：每帧 150ms → 16.7ms（满帧）
- 画面和片基长在一起，更像真胶片

## [1.11.2] — 2026-08-23

- 「胶卷是连续的」这条终于做到了：片身切成薄片按弧长摆位，画仍是整格，不变形

## [1.11] — 2026-08-23

- 胶卷改成连续多曲：整条带子一路蛇行，两端退进黑里

## [1.10.1] — 2026-08-23

- 胶卷**退回 v1.9.1 的做法** —— v1.10 在静态截图上更像实物，用起来更糟

## [1.10.0] — 2026-08-23 · 已回退

- 胶卷推翻重做（切片方案）

## [1.9.1] — 2026-08-23

- 胶卷参照实物重做：转角不再均匀，片门附近几乎是平的，出了这一段迅速拧向纵深

## [1.9] — 2026-08-22

- 胶卷改成曲面走带，齿孔与片基长在每一格自己身上

## [1.8] — 2026-08-22

- **新增「胶卷」模式**，呈现顺序改为 展墙 → 环形长廊 → 胶卷 → 沉浸式 → 瀑布流
- 展墙墙签补上原名（中文界面排原文名，英文界面排中文名）
- 核对全部中文译名，改掉 5 处误译
